// Configure your import map in config/importmap.rb. Read more: https://github.com/rails/importmap-rails
import "@hotwired/turbo-rails"
import "controllers"

// No need for the following line, as `rails-ujs` is no longer used
// import Rails from "@rails/ujs"

// Turbo automatically handles most of what `rails-ujs` used to do, so no need to call Rails.start() or Turbo.session.drive.
